# TimeLine example

This example demonstrates the FlowUI time line component:

- basic vertical events;
- alternate mode and custom marker colors;
- pending and loading events;
- custom icons and horizontal orientation;
- reverse ordering.

Run it with:

```powershell
go run ./examples/timelines
```
